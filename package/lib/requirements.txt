#
# Runtime dependencies vendored into package/lib/ by ucc-gen at build time so the
# modular inputs can import them inside Splunk's bundled Python.
#
# splunktaucclib >= 8.2.0 is required by the current ucc-gen (>=6.5.2); the old
# ==8.0.0 pin broke `ucc-gen build`. splunk-sdk is capped < 3.0.0 because 3.0.0
# requires Python >= 3.13 and would not install on the 3.9 build / break the
# Splunk 10 3.9 runtime. This matches the set proven green on TA-airspace-watch /
# TA-uk-open-data.
#
# requests is capped < 2.33.0: 2.33.0+ sets Requires-Python >= 3.10 and 2.34.0
# annotates _internal_utils with runtime `str | bytes` unions, which raise
# TypeError at import on the Splunk 10 3.9 runtime. unifi_input_helper imports
# requests at module top, so scheme introspection loads it under 3.9 and every
# collector fails to register. 2.32.5 is the last 3.9-safe release.
#
requests>=2.32.0,<2.33.0
splunk-sdk>=2.0.2,<3.0.0
splunktaucclib>=8.2.0
solnlib>=5.5.0
